TODO:
-Subject subscription list syncs grid enemy vector
-which class inherits from observer and which from subject?
-call fn on Subject instead of PC would cause parameter type error
-how to notify dragon? Option 1: add dragonhoard(gold) to observer lst  Option 2: dragon keeps a ptr to its gold
-implemented temporary merchant notify

-check isDead() in notifyObservers
